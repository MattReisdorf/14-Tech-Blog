{{#each posts as |post| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{post.title}} {{post.user}}</h2>
  </div>
  <div class="col-md-6">
    <p>{{post.text}}</p>
  </div>
</div>
{{/each}}
